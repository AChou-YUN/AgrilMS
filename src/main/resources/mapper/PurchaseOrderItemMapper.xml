<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.demo222.mapper.PurchaseOrderItemMapper">

    <resultMap id="BaseResultMap" type="org.example.demo222.entity.PurchaseOrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="quantity" property="quantity"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="subtotal" property="subtotal"/>
        <result column="production_date" property="productionDate"/>
        <result column="batch_number" property="batchNumber"/>
    </resultMap>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT poi.*, p.name AS product_name, p.specification, un.name AS unit_name
        FROM purchase_order_item poi
        LEFT JOIN product p ON poi.product_id = p.id
        LEFT JOIN unit un ON p.unit_id = un.id
        WHERE poi.order_id = #{orderId}
    </select>

    <insert id="insert" parameterType="org.example.demo222.entity.PurchaseOrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO purchase_order_item (order_id, product_id, quantity, unit_price, subtotal, production_date, batch_number)
        VALUES (#{orderId}, #{productId}, #{quantity}, #{unitPrice}, #{subtotal}, #{productionDate}, #{batchNumber})
    </insert>

    <insert id="batchInsert" parameterType="list">
        INSERT INTO purchase_order_item (order_id, product_id, quantity, unit_price, subtotal, production_date, batch_number)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.quantity}, #{item.unitPrice}, #{item.subtotal}, #{item.productionDate}, #{item.batchNumber})
        </foreach>
    </insert>

    <delete id="deleteByOrderId">
        DELETE FROM purchase_order_item WHERE order_id = #{orderId}
    </delete>

</mapper>