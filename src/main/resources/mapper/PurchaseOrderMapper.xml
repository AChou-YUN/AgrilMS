<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.demo222.mapper.PurchaseOrderMapper">

    <resultMap id="BaseResultMap" type="org.example.demo222.entity.PurchaseOrder">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="supplier_id" property="supplierId"/>
        <result column="user_id" property="userId"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="order_date" property="orderDate"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="supplier_name" property="supplierName"/>
        <result column="operator_name" property="operatorName"/>
    </resultMap>

    <insert id="insert" parameterType="org.example.demo222.entity.PurchaseOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO purchase_order (order_no, supplier_id, user_id, total_amount, status, order_date, remark)
        VALUES (#{orderNo}, #{supplierId}, #{userId}, #{totalAmount}, #{status}, #{orderDate}, #{remark})
    </insert>

    <update id="updateStatus">
        UPDATE purchase_order SET status = #{status} WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT po.*, s.name AS supplier_name, u.real_name AS operator_name
        FROM purchase_order po
        LEFT JOIN supplier s ON po.supplier_id = s.id
        LEFT JOIN sys_user u ON po.user_id = u.id
        WHERE po.id = #{id}
    </select>

    <select id="selectByOrderNo" resultMap="BaseResultMap">
        SELECT po.*, s.name AS supplier_name, u.real_name AS operator_name
        FROM purchase_order po
        LEFT JOIN supplier s ON po.supplier_id = s.id
        LEFT JOIN sys_user u ON po.user_id = u.id
        WHERE po.order_no = #{orderNo}
    </select>

    <select id="selectByPage" resultMap="BaseResultMap">
        SELECT po.*, s.name AS supplier_name, u.real_name AS operator_name
        FROM purchase_order po
        LEFT JOIN supplier s ON po.supplier_id = s.id
        LEFT JOIN sys_user u ON po.user_id = u.id
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND po.order_no LIKE CONCAT('%', #{orderNo}, '%')
            </if>
            <if test="supplierId != null">
                AND po.supplier_id = #{supplierId}
            </if>
            <if test="status != null">
                AND po.status = #{status}
            </if>
            <if test="startDate != null">
                AND po.order_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND po.order_date &lt;= #{endDate}
            </if>
        </where>
        ORDER BY po.create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*)
        FROM purchase_order po
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND po.order_no LIKE CONCAT('%', #{orderNo}, '%')
            </if>
            <if test="supplierId != null">
                AND po.supplier_id = #{supplierId}
            </if>
            <if test="status != null">
                AND po.status = #{status}
            </if>
            <if test="startDate != null">
                AND po.order_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND po.order_date &lt;= #{endDate}
            </if>
        </where>
    </select>

    <select id="countBySupplierId" resultType="int">
        SELECT COUNT(*) FROM purchase_order WHERE supplier_id = #{supplierId}
    </select>

    <select id="getMaxSequence" resultType="java.lang.Integer">
        SELECT MAX(CAST(SUBSTRING(order_no, 11) AS UNSIGNED))
        FROM purchase_order
        WHERE order_no LIKE CONCAT('PO', #{datePrefix}, '%')
    </select>

</mapper>